{
  "name": "signal-bloom-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Project site for Signal Bloom — an artifact generated by the system itself.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
