node_modules/
dist/
*.log
.DS_Store

# HEVC video master(s) — kept out of public/ so Vite never copies them into
# dist, and out of git. The web-ready hero.mp4 (transcoded ~27MB H.264) IS
# committed so Render can serve it. Re-transcode source lives in _masters/.
_masters/

# Raw PNG exports — production assets are the WebP versions in this directory.
# WebPs are committed; PNGs stay local only.
public/frames/bloom-*.png

# Visual Studies source exports (M1-*.png …) — the page serves the
# catalog-named WebPs (sb-i-01.webp …) converted from these.
public/studies/M*.png
